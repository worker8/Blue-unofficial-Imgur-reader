<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="worker8.com.github.imgurdiscovery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".ImgurDiscoveryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/imgur_discovery_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".public_activities.GifActivity"
            android:label="Gif Activity"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name="worker8.com.github.imgurdiscovery.public_activities.ImageActivity"
            android:label="Image Viewer"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".imgur_album.ImgurAlbumActivity"
            android:label="Imgur Album"
            android:theme="@style/ImgurAlbumTheme" />
    </application>

</manifest>
